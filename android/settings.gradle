pluginManagement {
  repositories {
    gradlePluginPortal()
    google()
  }
}

rootProject.name = "GCAndroidNativeModule"

include ':SDK'
project(':SDK').projectDir = new File(rootDir, 'GCoreVideoCallsSDK/')
